package io.voxellate.github;    //Defines the package, which is used to group the classes together

public class  MegamanRush { //Defines the class
    public static void main(String[] args) {    //The main method is called when the program starts
        new gameController();   //Creates a new instance of the gameController class and executes the class constructor
    }
}
